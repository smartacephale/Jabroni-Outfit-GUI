import type { StoreStateOptions } from '../types';

export const StoreStateDefault: StoreStateOptions = {
  uiEnabled: true,
  hidden: false,
  darkmode: true,
  $paginationLast: 1,
  $paginationOffset: 1,
};
